from setuptools import setup, find_packages

setup(
    name="debugkit",
    version="0.1.0",
    description="Simple debugging toolkit for Python",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.8",
)
